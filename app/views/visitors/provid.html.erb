<p id="notice">
  <%= notice %>
</p>

<h3 class="text-center content-header">
  <%= I18n.t "layout.specialization.provid_header" %>
</h3>

<ul class="nav nav-tabs nav-justified" role="tablist">
  <% @specializations.each_with_index do |specialization, index| %>
    <li class= <%= "active" if index == 0%> >
      <a href="#<%=specialization.name%>" data-toggle="tab" role="tab">
        <%= specialization.name %>
      </a>
    </li>
  <% end %>
</ul>

<br />

<div class="tab-content">
  <% @specializations.each_with_index do |specialization, index| %>
    <div id="<%=specialization.name%>" class="tab-pane <%= "active" if index == 0%>">
      <%= raw specialization.specdatum.provid %>
    </div>
  <% end %>
</div>