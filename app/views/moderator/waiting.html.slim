h3.text-center.content-header = I18n.t "layout.moderator.qualifications_page"

= render partial: "local_tabs", locals: {selected: NEEDS_APPROVE}

br/
br/

#waiting.tab-pane.active
  .qualifications-list = render partial: 'qualifications', locals: {selected: NEEDS_APPROVE}